# Assets Directory

## Adding Your Profile Picture

To replace the placeholder with your actual profile picture:

1. Save your profile photo as `profile.jpg` in this directory
2. Recommended size: 400x400px or larger (square format)
3. Supported formats: JPG, PNG, WebP

The footer will automatically display your profile picture with the animated gradient border effect.

Currently using `profile.svg` as a placeholder.
